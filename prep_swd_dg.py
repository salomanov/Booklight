import os

task = """Пользователь спросил: 'таккой вопрос ты сейчас без прошивки можешь общаться с контроллером'.
Нужно ответить:
1. Да, на 100%! Именно для этого и существует шина SWD (Serial Wire Debug), по которой сейчас подключен ST-Link.
2. Объяснить, как это работает: SWD дает прямой доступ к внутренней шине процессора (Cortex-M0+ AHB-Lite) и оперативной памяти (SRAM), не затрагивая Flash-память.
3. Перечислить конкретные практические возможности:
   - Чтение датчиков и ножек на лету (регистры GPIO IDR) - можно в реальном времени смотреть, нажат ли сенсор M+ (PB5), видит ли плата зарядку Type-C.
   - Чтение переменных прошивки из RAM (напряжение АКБ, проценты, флаги).
   - Прямое управление выводами (запись в GPIO BSRR/ODR) - можно командой с ПК включить подсветку, послать команду на экран, проверить мосфет.
   - Остановка (Halt), чтение регистров ядра и возобновление (Resume) без перезагрузки.
4. Предложить конкретный живой тест (например, прочитать напряжение или состояние сенсора прямо сейчас)."""

draft = """**Да, на 100%!** Именно в этом главная сила отладочного интерфейса **SWD (Serial Wire Debug)**, через который мы подключены.

Для общения с микроконтроллером **перепрошивать его НЕ нужно**. Через два провода (`SWCLK` и `SWDIO`) программатор имеет прямой аппаратный доступ к системной шине процессора (Coresight DAP) в реальном времени, не изнашивая и не трогая Flash-память.

---

### Что мы можем делать прямо сейчас «на лету»:

1. **Читать состояние любых ножек в реальном времени (входные регистры `GPIOx->IDR`):**
   * Можем непрерывно мониторить пин сенсора `PB5` (`M+`): ты касаешься сенсора пальцем — я в ту же миллисекунду вижу изменение бита на экране ПК.
   * Можем смотреть сигналы на портах Type-C (`PB4`, `PB3`), проверяя, видит ли плата зарядку физически.

2. **Читать переменные прошивки из оперативной памяти (RAM):**
   * Переменные `bat_millivolts`, `bat_percent`, `lamp_on`, `current_brightness` лежат по фиксированным адресам в ОЗУ.
   * Я могу считывать их как живую телеметрию (например, строить график напряжения или смотреть точный замер АЦП раз в 100 мс).

3. **Управлять платой напрямую с компьютера (выходные регистры `GPIOx->BSRR` / `ODR`):**
   * Могу подать команду с ПК, и плата зажжёт светодиод филамента или переключит фары на экране, вообще без нажатия кнопок.

4. **Запускать микропрограммы в оперативной памяти (SRAM):**
   * Если нужно протестировать какую-то гипотезу (например, прозвонить неизвестную дорожку), можно загрузить 20 байт кода прямо в ОЗУ чипа и выполнить их без прошивки.

5. **Останавливать ядро (Halt) и инспектировать регистры:**
   * Поставить процессор на паузу, посмотреть, какую конкретно инструкцию он выполняет прямо сейчас, и снять с паузы.

---

👉 **Хочешь протестировать прямо сейчас?**  
Например, я могу запустить скрипт живого монитора: ты будешь касаться контакта кнопки `M+` или втыкать Type-C, а мы увидим в реальном времени, как меняются регистры микроконтроллера!"""

temp = os.environ.get('TEMP', r'C:\Users\Salomanov\AppData\Local\Temp')
with open(os.path.join(temp, 'dg-task.txt'), 'w', encoding='utf-8') as f:
    f.write(task)
with open(os.path.join(temp, 'dg-draft.md'), 'w', encoding='utf-8') as f:
    f.write(draft)

print('Task and draft ready')
