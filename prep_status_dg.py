import os

task = """Пользователь спросил: 'смотри в каом сейчас состоянии контроллер, визически сейчас к нему не подключена ни кнопка ни зарядка не батарея, питание чисто от програматора, а контроллел что видит'.
Нужно разобрать по полочкам с точки зрения физики, схемотехники и кода:
1. Питание: контроллер получает ровно 3.30V от LDO программатора. Для литиевой батареи это глубокий разряд, поэтому АЦП пересчитывает 3.3V в 1% заряда.
2. Экран: показывает '01%', горит только 1 деление шкалы, фары светятся КРАСНЫМ (индикатор критического разряда < 25%).
3. Зарядка: Type-C пуст, напряжение 3.3V < 4.18V порога, PB4 подтянут PULLUP к 3.3V (ключ LP4054 закрыт), PB3 = 0V. Контроллер видит is_charging = false, поэтому значок молнии ПОГАШЕН, шкала НЕ бежит.
4. Кнопка: пятачок M+ висит в воздухе, но в коде включен PULLDOWN к земле, поэтому контроллер видит стабильный 0 (кнопка отпущена). Лампа выключена, ШИМ = 0, на затворах PA0/PA5 нули.
Спросить пользователя, сходится ли эта картина с тем, что он видит на экране прямо сейчас."""

draft = """Вот полная физическая и программная картина того, что прямо сейчас «видит» и думает контроллер:

---

### 1. По питанию и батарее (Главная иллюзия контроллера):
* **Что происходит физически:** контроллер питается не от аккумулятора 3.7–4.2 В, а от стабилизатора 3.3 В программатора ST-Link. Напряжение шины `VCC` равно ровно **3.30 В**.
* **Что видит встроенный 12-битный АЦП:** измеряя внутренний Bandgap 1.20 В, АЦП честно фиксирует: напряжение питания равно **3300 мВ**.
* **Что решает прошивка:** для Li-ion аккумулятора 3.3 В — это состояние глубокого разряда (порог отключения банки). По калибровочной таблице это соответствует **1% заряда**.
* **Что на экране:** 
  * Горит цифра **`01%`** (или 1%).
  * Горит **1 нижнее деление шкалы**.
  * Фары-глаза горят **КРАСНЫМ цветом** (индикатор разряда ниже 25%).

---

### 2. По зарядке Type-C:
* **Что происходит физически:** кабель Type-C не воткнут, чип заряда LP4054 обесточен.
* **Что видит контроллер:**
  1. Напряжение шины питания 3.30 В намного ниже порога зарядки 4.18 В (`v_high = false`).
  2. Вывод статуса заряда `PB4` подтянут внутренним резистором к 3.3 В (`GPIO_PULLUP`), и так как микросхема заряда не активна, на пине держится логическая 1 (`p_chrg = false`).
  3. На входе шины 5V `PB3` чистый 0 В.
* **Итог:** контроллер видит **«Работа от батареи, зарядки нет»** (`is_charging = false`).
  * Значок молнии ⚡ **погашен**.
  * Шкала **не анимируется** (статична).

---

### 3. По сенсорной кнопке (пятачок `M+` / `PB5`):
* **Что происходит физически:** модуль TTP223 ещё не припаян, пятачок `M+` висит в воздухе.
* **Что видит контроллер:** на входе `PB5` мы программно включили внутренний резистор подтяжки к земле (`GPIO_PULLDOWN`). Поэтому на входе микроконтроллера держится стабильный **железный ноль (LOW)** без наводок.
* **Итог:** кнопка видна как **«ОТПУЩЕНА»** (касания нет).
  * Лампа выключена (`lamp_on = false`).
  * На выходах полевых транзисторов `PA0` (ножка 13) и `PA5` (ножка 18) уровень **0 В (GND)**, мосфеты наглухо закрыты.

---

👀 **Посмотри сейчас на дисплей:** он показывает **`01%`**, одно нижнее деление и красные фары? Всё сходится?"""

temp = os.environ.get('TEMP', r'C:\Users\Salomanov\AppData\Local\Temp')
with open(os.path.join(temp, 'dg-task.txt'), 'w', encoding='utf-8') as f:
    f.write(task)
with open(os.path.join(temp, 'dg-draft.md'), 'w', encoding='utf-8') as f:
    f.write(draft)

print('Task and draft ready')
