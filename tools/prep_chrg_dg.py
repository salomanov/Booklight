import os

task = """Пользователь сообщил: 'сейчас горят оба синим, 01% молния капля, полоски красная горит, желтая синяя и зелёна зогораются одна за другой и потом вместе гаснут' при питании 3.3V от программатора.
Нужно объяснить:
1. Это отличный результат: экран, 1-Wire протокол, АЦП (3.3V = 01%) и бегущая анимация зарядки (1..4 полоски по очереди) работают на 100% безупречно!
2. Объяснить корень причины, почему включился режим зарядки:
   - В коде было условие p_chrg = (PB4 == RESET), где PB4 опрашивался на ноль (в предположении open-drain чарджера).
   - Но на плате CXV0257 вывод PB4 не соединен с чарджером и на нем всегда 0V (GND). Поэтому условие (0 == 0) постоянно истинно!
3. Как сделать правильно и навсегда:
   - Убрать опрос PB4 и оставить детектор по напряжению АЦП: is_charging = (bat_millivolts >= 4180).
   - Тогда при 3.3V от программатора (или 3.99V от батареи) экран будет в нормальном режиме (статичная шкала, молния выключена, зеленые/красные фары).
   - А при втыкании кабеля Type-C напряжение поднимается до 4.24V, и эта анимация включится сама!"""

draft = """Это **потрясающий результат!** Экран, протокол 1-Wire, АЦП и сама анимация работают **на 100% идеально!** 🎯🔥

Смотри, что прямо сейчас произошло:
1. **`01%`** — АЦП честно измерил 3.30 В от программатора и пересчитал их в 1% (для лития 3.3 В — это разряд).
2. **Анимация шкалы (красная → желтая → синяя → зеленая по очереди)** и **синие/циановые фары** — это именно та самая анимация зарядки, которую мы заложили в прошивку!

---

### Почему контроллер решил, что сейчас идёт зарядка:

В прошивку мы добавили проверку:
```c
bool p_chrg = (HAL_GPIO_ReadPin(GPIOB, GPIO_PIN_4) == GPIO_PIN_RESET);
```
Мы рассчитывали, что контроллер заряда LP4054 при зарядке притягивает пин к нулю (`RESET`).

Но на плате **`CXV0257-V1.3`** вывод `PB4` не подключён к чарджеру — на нём и так **всегда висит физический ноль (GND)**!  
В итоге условие `PB4 == 0` сработало (`0 == 0 -> TRUE`), и контроллер посчитал, что зарядка подключена прямо сейчас!

---

### Как мы делаем чисто и красиво:

На плате `CXV0257-V1.3` самый точный и железный детектор зарядки — это **напряжение на аккумуляторе (АЦП)**:
* **Питание от программатора:** 3.30 В (< 4.18 В) → **Зарядки нет**.
* **Батарея iPhone в покое:** 3.99 В (< 4.18 В) → **Зарядки нет** (экран показывает 85%, статичная шкала, зелёные фары, молния выключена).
* **Втыкаем кабель Type-C:** напряжение мгновенно подскакивает до **4.239 В** (≥ 4.18 В) → **Включается молния, синие фары и эта бегущая анимация!**

Я сейчас за 5 секунд уберу ложный опрос `PB4` из кода, пересоберу и прошью. Нажать «прошить»?"""

temp = os.environ.get('TEMP', r'C:\Users\Salomanov\AppData\Local\Temp')
with open(os.path.join(temp, 'dg-task.txt'), 'w', encoding='utf-8') as f:
    f.write(task)
with open(os.path.join(temp, 'dg-draft.md'), 'w', encoding='utf-8') as f:
    f.write(draft)

print('Task and draft ready')
