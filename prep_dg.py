import os

task = """Пользователь попросил: 'заливай полную прошивку, буду тестировать, и кнопку припаяю'.
Нужно:
1. Сообщить, что полная прошивка полностью собрана (custom_firmware/build/battery_firmware.bin, 5304 байт, 21% Flash).
2. Описать все функции, которые вошли в прошивку:
   - Дисплей всегда горит (PA1): реальный % заряда от АЦП, шкала из 4 делений, цветные фары.
   - Автодетект Type-C: молния + бегущая шкала 1..4 + циан фары при подключении зарядки (по скачку напряжения > 4.18V и пинам).
   - Сенсорная кнопка (TTP223 на M+ / PB5):
     * Короткий тап: плавное включение/выключение лампы (память яркости).
     * Удержание: плавный димминг 5..100% с гамма-коррекцией.
     * Во время настройки яркости на экране на 2 секунды отображается % яркости лампы, затем возврат к % батареи.
     * Таймер 15 минут с минутным угасанием.
   - Двойной ШИМ 200 Гц на PA0 (пин 13) и PA5 (пин 18) для силового ключа лампы.
   - Защита SWD: всегда активен.
3. Объяснить текущее состояние программатора:
   - USB-свисток программатора обнаружен (Segger J-Link STLink).
   - Но линии SWD от программатора к плате сейчас отключены (ты сам их отключал ранее).
   - Чтобы прошить, нужно подсоединить 3 проводка к плате (GND, CK, DA) и дать команду, либо запустить наш скрипт ожидания."""

draft = """🎉 **Полная прошивка собрана и готова к заливке!**  
Файл: [`battery_firmware.bin`](file:///c:/Users/Salomanov/Desktop/ВЕЙП/custom_firmware/build/battery_firmware.bin) (5304 байт, занято всего 21% памяти).

---

### 📦 Что вошло в финальную прошивку Booklight:

1. **Экран всегда включён (1-Wire `PA1` / Pad 1):**
   * **Постоянная индикация батареи:** реальный заряд 0..100% от 12-битного АЦП (VREFINT 1.20V Bandgap).
   * **Шкала делений:** 1..4 сегмента пропорционально заряду.
   * **Фары-глаза:** Зелёный (>60%), Жёлтый (25..59%), Красный (<25%).

2. **Зарядка Type-C (Автоопределение):**
   * **Индикаторы:** зажигается **молния** ⚡, запускается **бегущая шкала** (1 → 2 → 3 → 4) и **циановые фары**.
   * **Детектор:** срабатывает мгновенно по физическому скачку напряжения на аккумуляторе до **4.20–4.24 В** (плюс аппаратный опрос пинов `PB4`/`PB3`).

3. **Сенсорная кнопка TTP223 (вход `PB5` / площадка `M+`):**
   * **Короткий тап (< 350 мс):** плавное включение / выключение лампы (~300 мс) с сохранением выбранного уровня.
   * **Удержание пальца (> 350 мс):** плавный димминг **5% .. 100%** с человеческой перцептивной гамма-кривой CIE 1931 (без мерцания на 200 Гц). Направление димминга («ярче» / «темнее») меняется при каждом следующем удержании.
   * **Обратная связь на экране:** во время удержания и ещё 2 секунды после отпускания дисплей показывает точный **% яркости лампы**, а затем сам возвращается к показу заряда батареи!
   * **Автотаймер чтения (15 мин):** если лампа горит 15 минут без нажатий, запускается мягкое 60-секундное угасание. Тап в это время отменяет отключение.

4. **Силовой выход на филамент 3В (MOSFET):**
   * ШИМ дублируется одновременно на **`PA0` (ножка 13)** и **`PA5` (ножка 18)** — лампа засветится независимо от того, на какую из этих ножек разведён затвор транзистора на плате `CXV0257`.

5. **SWD всегда на связи:**
   * Отладочные пины `SWCLK` (`PA2`) и `SWDIO` (`PB6`) никогда не блокируются, прошивать можно в любой момент.

---

### 🔌 Состояние программатора прямо сейчас:

USB-свисток программатора подключён к ПК (`Segger J-Link STLink`), но **провода к плате сейчас не соединены** (ранее ты их отключал).

**Для прошивки подключи к плате:**
1. **`GND`** — общий минус (на корпус Type-C или пятачок `GND`).
2. **`SWCLK`** — на пятачок **`CK`** (`PA2`).
3. **`SWDIO`** — на пятачок **`DA`** (`PB6`).
*(Если плата питается от своего аккумулятора — провод 3.3V подключать не обязательно, нужен только общий GND).*

Как только соединишь проводки — маякни мне (или я могу запустить автоловушку прямо сейчас), и мы зальём прошивку за 3 секунды!"""

temp = os.environ.get('TEMP', r'C:\Users\Salomanov\AppData\Local\Temp')
with open(os.path.join(temp, 'dg-task.txt'), 'w', encoding='utf-8') as f:
    f.write(task)
with open(os.path.join(temp, 'dg-draft.md'), 'w', encoding='utf-8') as f:
    f.write(draft)

print('Task and draft ready for Delegator')
